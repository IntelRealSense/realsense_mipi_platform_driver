From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: ejgoldik <ehud.joseph.goldik@realsenseai.com>
Date: Mon, 7 Sep 2026 12:00:00 +0300
Subject: [PATCH] media: tegra: extended format rows: datatype override, wire
 geometry, variable height

Let a format-table row (TEGRA_VIDEO_FORMAT_EXTENDED) declare a datatype
override, a transport geometry and behaviour flags: the VI substitutes
the row's dt_override for the incoming pixel datatype (matching must be
disabled - dt_enable never matches a frame otherwise), pairs it with an
explicit memory writer, and scales frame_x/frame_y and sizeimage by the
row's wire-geometry fractions, rounding frame_x up to the line padding
the transmitter applies.

Four rows use the override/geometry fields. The D58x calibration streams
(RGB GRBG10 as V4L2_PIX_FMT_SGRBG16 'GR16', IR Y16I) transmit LSB-first
RAW16, which the NVCSI RAW16 rule byte-swaps (CSI-2 defines RAW16
MSB-first); both override to YUV422-8 + T_Y8_U8__Y8_V8 for
byte-identical little-endian capture, and Y16I declares frame_x_scale =
2/1 (32bpp L/R pair on a 16bpp wire), replacing the fourcc-gated width
doubling formerly in the 0001 patches. Flat NV12 rides a RAW8 wire
carrier with native matching (a geometry-only row; the MAX96717 pixel
pipe forwards standard 8-bit DTs but drops user-defined ones) and
frame_y_scale = 3/2 transport rows, replacing the per-format special
cases of the earlier flat-NV12 receive path. The D401 CSI-PT stream is
a geometry-only row too: packed 10-bit BGGR, 5 bytes per 4 pixels, that
the VDF dword-aligns per line and sends over a RAW8 carrier at one wire
pixel per byte - bpp and frame_x_scale 5/4 with frame_x_align 4, which
gives 1288 px -> 1612 wire bytes and stays correct at any width. The
d4xx driver can then advertise, and hand the FW, the true pixel width
instead of the transport byte count. Encoding the padding as a ratio
(1612/1288) would have been exact only at that one resolution.

A fifth row uses the flags. Streams that multiplex differently sized
payloads on one virtual channel (compressed images, perception blobs)
keep a constant line length but end each frame after a per-frame number
of lines; the RTCPU reports such a frame with err_data ==
CAPTURE_VI_ENGINE_STATUS_PIXEL_INCOMPLETE and vi5_capture_dequeue()
completed it as VB2_BUF_STATE_ERROR. TEGRA_VIDEO_FORMAT_FLAG_VARIABLE_HEIGHT
on the "Variable-length data" RAW8 row (MEDIA_BUS_FMT_RS_VARLEN_1X8 /
V4L2_PIX_FMT_RS_VARLEN, kernel 0006) completes a frame whose err_data is
exactly PIXEL_INCOMPLETE, channel not in error, as VB2_BUF_STATE_DONE.
Width is the fixed bytes per line and height the worst-case line count,
so sizeimage is the largest frame the channel can deliver; bytesused
still reports sizeimage and the application takes the payload length
from an in-band header or the metadata. Any other fault, or
PIXEL_INCOMPLETE combined with another fault, keeps the error path; rows
without the flag are unchanged. err_data follows the
CAPTURE_VI_ENGINE_STATUS_* layout: a short frame reports err_data bit 17
together with notify_bits bit 42 (CHANSEL_PIX_SHORT), HW-confirmed on
JP6.2.

HW-verified (JP6.2, fw-orin-5, D585): GR16 30/30 clean frames, Y16I
5/5, LE payload on both, coexists with concurrent embedded-metadata
capture; NV12 10/10 @640x360 and 5/5 @1280x720, exact 1.5*bpl*H frames.

Signed-off-by: ejgoldik <ehud.joseph.goldik@realsenseai.com>
---
  .../media/platform/tegra/camera/vi/channel.c       | 11 ++++
  .../media/platform/tegra/camera/vi/vi5_fops.c      | 31 +++++++++++
  .../media/platform/tegra/camera/vi/vi5_formats.h   | 49 +++++++++++++++++
  include/media/tegra_camera_core.h    | 64 ++++++++++++++++++++++
  4 files changed, 155 insertions(+)

diff --git a/drivers/media/platform/tegra/camera/vi/channel.c b/drivers/media/platform/tegra/camera/vi/channel.c
index ac5e0e9..bedcaf9 100644
--- a/drivers/media/platform/tegra/camera/vi/channel.c
+++ b/drivers/media/platform/tegra/camera/vi/channel.c
@@ -192,6 +192,9 @@ static void tegra_channel_fmt_align(struct tegra_channel *chan,
 	fmt_align = (denominator == 1) ? numerator : 1;
 	align = lcm(chan->width_align, fmt_align);
 	align = align > 0 ? align : 1;
+	/* Keep the stride at least as long as a padded wire line. */
+	if (vfmt->frame_x_align > 1)
+		align = lcm(align, (unsigned int)vfmt->frame_x_align);
 	bpl = tegra_core_bytes_per_line(*width, align, vfmt);
 
 	/* Align stride */
@@ -295,6 +298,10 @@ static void tegra_channel_update_format(struct tegra_channel *chan,
 
 	if (fourcc == V4L2_PIX_FMT_NV16)
 		chan->format.sizeimage *= 2;
+
+	/* Transport rows may exceed V4L2 rows (tegra_camera_core.h). */
+	chan->format.sizeimage = tegra_frac_scale(chan->format.sizeimage,
+					&chan->fmtinfo->frame_y_scale);
 }
 
 static void tegra_channel_fmts_bitmap_init(struct tegra_channel *chan)
@@ -2129,6 +2136,10 @@ __tegra_channel_try_format(struct tegra_channel *chan,
 	if (chan->fmtinfo->fourcc == V4L2_PIX_FMT_NV16)
 		pix->sizeimage *= 2;
 
+	/* Transport rows may exceed V4L2 rows (tegra_camera_core.h). */
+	pix->sizeimage = tegra_frac_scale(pix->sizeimage,
+					  &vfmt->frame_y_scale);
+
 	return ret;
 }
 
diff --git a/drivers/media/platform/tegra/camera/vi/vi5_fops.c b/drivers/media/platform/tegra/camera/vi/vi5_fops.c
index 487ea19..7f4e64d 100644
--- a/drivers/media/platform/tegra/camera/vi/vi5_fops.c
+++ b/drivers/media/platform/tegra/camera/vi/vi5_fops.c
@@ -407,6 +407,15 @@ static void vi5_setup_surface(struct tegra_channel *chan,
 		offset = buf->addr + chan->buffer_offset[1 - vi_port];
 	}
 
+	/* Rows may scale the transport geometry (tegra_camera_core.h):
+	 * frame_x/frame_y count wire pixels/rows, which can differ from the
+	 * V4L2 geometry (e.g. Y16I: 32bpp pixels on a 16bpp RAW16 wire).
+	 */
+	width = tegra_frac_scale(width, &chan->fmtinfo->frame_x_scale);
+	if (chan->fmtinfo->frame_x_align > 1)
+		width = roundup(width, chan->fmtinfo->frame_x_align);
+	height = tegra_frac_scale(height, &chan->fmtinfo->frame_y_scale);
+
 	memcpy(desc, &capture_template, sizeof(capture_template));
 	memset(desc_memoryinfo, 0, sizeof(*desc_memoryinfo));
 
@@ -420,6 +429,20 @@ static void vi5_setup_surface(struct tegra_channel *chan,
 	desc->ch_cfg.pixfmt_enable = 1;
 	desc->ch_cfg.pixfmt.format = format;
 
+	/* Formats may declare a datatype override (tegra_camera_core.h) to
+	 * substitute the parsing rule for the incoming pixel packets.
+	 * DT matching must be disabled: dt_enable never matches a frame
+	 * with matching left on.
+	 */
+	if (chan->fmtinfo->dt_override) {
+		desc->ch_cfg.match.datatype = 0;
+		desc->ch_cfg.match.datatype_mask = 0;
+		desc->ch_cfg.dt_enable = 1;
+		desc->ch_cfg.dt_override = chan->fmtinfo->dt_override;
+		if (chan->fmtinfo->pixfmt_override)
+			desc->ch_cfg.pixfmt.format = chan->fmtinfo->pixfmt_override;
+	}
+
 	desc_memoryinfo->surface[0].base_address = offset;
 	desc_memoryinfo->surface[0].size = chan->format.bytesperline * height;
 	desc->ch_cfg.atomp.surface_stride[0] = bpl;
@@ -590,6 +613,14 @@ static void vi5_capture_dequeue(struct tegra_channel *chan,
 				chan->queue_error = true;
 				dev_err(vi->dev, "uncorr_err: flags %d, err_data %d\n",
 					descr->status.flags, descr->status.err_data);
+			} else if ((chan->fmtinfo->flags &
+				    TEGRA_VIDEO_FORMAT_FLAG_VARIABLE_HEIGHT) &&
+				   descr->status.err_data ==
+				   CAPTURE_VI_ENGINE_STATUS_PIXEL_INCOMPLETE) {
+				/* Fewer lines than frame_y is the normal case for a
+				 * variable-length format; the payload is in memory. */
+				dev_dbg(vi->dev, "short frame delivered, frame %d\n",
+					descr->status.frame_id);
 			} else {
 				dev_warn(vi->dev,
 					"corr_err: discarding frame %d, flags: %d, "
diff --git a/drivers/media/platform/tegra/camera/vi/vi5_formats.h b/drivers/media/platform/tegra/camera/vi/vi5_formats.h
index 73c1d84..d5d4e8d 100644
--- a/drivers/media/platform/tegra/camera/vi/vi5_formats.h
+++ b/drivers/media/platform/tegra/camera/vi/vi5_formats.h
@@ -149,6 +149,55 @@ static const struct tegra_video_format vi5_video_formats[] = {
 	// R[7:3]R[3:0] | L[3:0]R[11:8] | L[11:8]L[7:4] | ALIGN[7:0]
 	TEGRA_VIDEO_FORMAT(RGB888, 24, RGB888_1X24, 4, 1, T_A8R8G8B8,
 				RGB888, Y12I, "Y12I 24"),
+	// D58x calib surfaces: LE 16-bit samples over RAW16, captured in wire
+	// byte order — the DT override bypasses the RAW16 rule's CSI-2
+	// MSB-first byte swap. Y16I is a 32bpp L/R pair riding a 16bpp wire,
+	// so its frame_x scale is 2 wire pixels per V4L2 pixel.
+	TEGRA_VIDEO_FORMAT_EXTENDED(RAW16, 16, ARGB8888_1X32, 4, 1, T_R16,
+				RAW16, Y16I, "Y16I 32 LE",
+				TEGRA_IMAGE_DT_YUV422_8,		/* dt_override */
+				TEGRA_IMAGE_FORMAT_T_Y8_U8__Y8_V8,	/* pixfmt_override */
+				2, 1,			/* frame_x scale */
+				1, 1,			/* frame_y scale */
+				0,			/* frame_x align */
+				0),			/* flags */
+	TEGRA_VIDEO_FORMAT_EXTENDED(RAW16, 16, SGRBG16_1X16, 2, 1, T_R16,
+				RAW16, SGRBG16, "GRBG16 16 LE",
+				TEGRA_IMAGE_DT_YUV422_8,		/* dt_override */
+				TEGRA_IMAGE_FORMAT_T_Y8_U8__Y8_V8,	/* pixfmt_override */
+				1, 1,			/* frame_x scale */
+				1, 1,			/* frame_y scale */
+				0,			/* frame_x align */
+				0),			/* flags */
+	// Flat NV12 bytes over a RAW8 wire carrier: native match and byte
+	// rule, one byte per wire pixel. Transport rows = H * 3/2.
+	TEGRA_VIDEO_FORMAT_EXTENDED(RAW8, 8, RS_NV12_FLAT_1X8, 1, 1, T_R8,
+				RAW8, NV12, "NV12 flat over RAW8",
+				0, 0,			/* no dt/pixfmt override */
+				1, 1,			/* frame_x scale */
+				3, 2,			/* frame_y scale */
+				0,			/* frame_x align */
+				0),			/* flags */
+	// D401 CSI-PT: packed 10-bit BGGR (5 bytes per 4 pixels) that the VDF
+	// dword-aligns per line, carried as RAW8 - one wire pixel per byte, so
+	// the same 5/4 and 4-byte padding give both bytesperline and frame_x.
+	TEGRA_VIDEO_FORMAT_EXTENDED(RAW8, 8, RS_SBGGR10P_1X8, 5, 4, T_R8,
+				RAW8, SBGGR10P, "BGGR10P dword-aligned",
+				0, 0,			/* no dt/pixfmt override */
+				5, 4,			/* frame_x scale */
+				1, 1,			/* frame_y scale */
+				4,			/* frame_x align */
+				0),			/* flags */
+	// Variable-length data over RAW8: width = fixed bytes per line, height
+	// = worst-case line count. A frame may end after fewer lines; the app
+	// finds the payload length in an in-band header or the metadata.
+	TEGRA_VIDEO_FORMAT_EXTENDED(RAW8, 8, RS_VARLEN_1X8, 1, 1, T_R8,
+				RAW8, RS_VARLEN, "Variable-length data",
+				0, 0,			/* no dt/pixfmt override */
+				1, 1,			/* frame_x scale */
+				1, 1,			/* frame_y scale */
+				0,			/* frame_x align */
+				TEGRA_VIDEO_FORMAT_FLAG_VARIABLE_HEIGHT),	/* flags */
 
 };
 
diff --git a/include/media/tegra_camera_core.h b/include/media/tegra_camera_core.h
index a4c6978..9447396 100644
--- a/include/media/tegra_camera_core.h
+++ b/include/media/tegra_camera_core.h
@@ -92,6 +92,24 @@ struct tegra_frac {
 	unsigned int denominator;
 };
 
+/**
+ * tegra_frac_scale - scale a transport dimension by a wire-geometry fraction
+ * @value: the V4L2-side dimension, in pixels, rows or bytes
+ * @f: the format row's fraction
+ *
+ * Multiplies before dividing so the ratio does not truncate. An unset
+ * fraction (zero numerator or denominator) and an identity ratio are both
+ * no-ops, so a zero denominator can never reach the division.
+ */
+static inline u32 tegra_frac_scale(u32 value, const struct tegra_frac *f)
+{
+	if (!f->numerator || !f->denominator ||
+	    f->numerator == f->denominator)
+		return value;
+
+	return (value * f->numerator) / f->denominator;
+}
+
 /**
  * struct tegra_video_format - Tegra video format description
  * @vf_code: video format code
@@ -102,6 +120,20 @@ struct tegra_frac {
  * @img_dt: image data type
  * @fourcc: V4L2 pixel format FCC identifier
  * @description: format description, suitable for userspace
+ * @dt_override: if nonzero, capture with CSI DT matching disabled and this
+ *		datatype substituted for the incoming pixel datatype
+ * @pixfmt_override: VI memory format paired with @dt_override (0 = keep
+ *		@img_fmt)
+ * @frame_x_scale: wire pixels per V4L2 pixel as a fraction; scales the
+ *		capture descriptor frame_x (zero numerator or denominator =
+ *		no scaling)
+ * @frame_y_scale: transport rows per V4L2 row as a fraction; scales the
+ *		capture descriptor frame_y and surface size (zero numerator
+ *		or denominator = no scaling)
+ * @frame_x_align: granularity the transmitter pads each line up to, in wire
+ *		pixels; 0 and 1 both mean unpadded, being roundup no-ops.
+ *		The scaled frame_x rounds up to it, and it is folded into
+ *		the bytes-per-line alignment so the stride covers the line
  */
 struct tegra_video_format {
 	enum tegra_vf_code vf_code;
@@ -112,8 +144,18 @@ struct tegra_video_format {
 	enum tegra_image_dt img_dt;
 	u32 fourcc;
 	__u8 description[32];
+	u8 dt_override;
+	u8 pixfmt_override;
+	struct tegra_frac frame_x_scale;
+	struct tegra_frac frame_y_scale;
+	u8 frame_x_align;
+	u32 flags;
 };
 
+/* Frames may carry fewer lines than frame_y; the VI PIXEL_INCOMPLETE
+ * fault is then delivered as a good frame (payload carries its length). */
+#define TEGRA_VIDEO_FORMAT_FLAG_VARIABLE_HEIGHT		BIT(0)
+
 #define	TEGRA_VIDEO_FORMAT(VF_CODE, BPP, MBUS_CODE, FRAC_BPP_NUM,	\
 	FRAC_BPP_DEN, FORMAT, DATA_TYPE, FOURCC, DESCRIPTION)		\
 {									\
@@ -127,6 +169,28 @@ struct tegra_video_format {
 	DESCRIPTION,							\
 }
 
+#define	TEGRA_VIDEO_FORMAT_EXTENDED(VF_CODE, BPP, MBUS_CODE,		\
+	FRAC_BPP_NUM, FRAC_BPP_DEN, FORMAT, DATA_TYPE, FOURCC,		\
+	DESCRIPTION, DT_OVERRIDE, PIXFMT_OVERRIDE,			\
+	FRAME_X_NUM, FRAME_X_DEN, FRAME_Y_NUM, FRAME_Y_DEN,		\
+	FRAME_X_ALIGN, FLAGS)						\
+{									\
+	TEGRA_VF_##VF_CODE,						\
+	BPP,								\
+	MEDIA_BUS_FMT_##MBUS_CODE,					\
+	{FRAC_BPP_NUM, FRAC_BPP_DEN},					\
+	TEGRA_IMAGE_FORMAT_##FORMAT,					\
+	TEGRA_IMAGE_DT_##DATA_TYPE,					\
+	V4L2_PIX_FMT_##FOURCC,						\
+	DESCRIPTION,							\
+	DT_OVERRIDE,							\
+	PIXFMT_OVERRIDE,						\
+	{FRAME_X_NUM, FRAME_X_DEN},					\
+	{FRAME_Y_NUM, FRAME_Y_DEN},					\
+	FRAME_X_ALIGN,							\
+	FLAGS,								\
+}
+
 u32 tegra_core_get_word_count(unsigned int frame_width,
 		const struct tegra_video_format *fmt);
 u32 tegra_core_bytes_per_line(unsigned int width, unsigned int align,
